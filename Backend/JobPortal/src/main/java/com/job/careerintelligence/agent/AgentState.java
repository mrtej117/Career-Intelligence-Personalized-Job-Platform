package com.job.careerintelligence.agent;

public enum AgentState {
    IDLE,
    OBSERVING,
    COLLECTING,
    ANALYZING_CHANGES,
    ENRICHING,
    EMBEDDING,
    MATCHING,
    RANKING,
    VERIFYING,
    COMPLETED,
    FAILED,
    STOPPED
}
